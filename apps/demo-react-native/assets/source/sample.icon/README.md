# Icon Composer passthrough slot

Replace this directory with an `AppIcon.icon` package exported by Apple Icon
Composer before enabling `runtime.assetloom.example.json`. Assetloom copies the
package without interpreting or rewriting its proprietary contents.
