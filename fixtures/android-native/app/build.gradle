plugins {
    id "com.android.application"
}

android {
    namespace "dev.assetloom.fixture"
    compileSdk 36

    defaultConfig {
        applicationId "dev.assetloom.fixture"
        minSdk 24
        targetSdk 36
        versionCode 1
        versionName "1.0"
    }
}
